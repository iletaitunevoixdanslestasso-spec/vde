import { BaseService } from "./BaseService";


export class RepetitionstypeService extends BaseService {

    constructor(repository, validator, mapper) {
        super(repository, validator, mapper);
    }

}
